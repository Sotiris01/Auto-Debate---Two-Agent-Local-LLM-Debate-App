"""Per-result favourability filter — TODO (Phase 20).

This module will replace the loose ``supports/contradicts/irrelevant``
summariser with a stance-aware filter that defaults to *discard* when
uncertain and tags every kept hit with the claim it supports plus a
source-kind classification.

See ``ROADMAP.md`` Phase 20 for the full deliverables and exit criteria.
"""

from __future__ import annotations

# TODO: Phase 20 — implement FilteredHit and filter_result().

__all__: list[str] = []
