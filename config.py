"""
config.py — Typed runtime configuration loaded from environment / .env.

Part of the Auto Debate project. See PROJECT.md and ROADMAP.md.
"""

# TODO(phase-2): define the frozen `Settings` dataclass with fields
#   ollama_host, model_name, max_turns, temperature, top_p, word_limit.
# TODO(phase-2): implement `load_settings()` using python-dotenv +
#   os.getenv with typed defaults and validation.
# TODO(phase-2): define `ConfigError` raised with concatenated validation
#   problems.
# TODO(phase-7): add a small logging configuration helper.
